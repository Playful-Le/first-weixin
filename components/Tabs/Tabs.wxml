<view class="tabs">
    <view class="tabs_title {{item.isActive?'active':''}}"
        wx:for="{{tabs}}"
        wx:key="id"
        bind:tap="handleItemTap"
        data-index="{{index}}"
    >
        {{item.value}}
    </view>
    <view class="tabs_content"></view>
</view>